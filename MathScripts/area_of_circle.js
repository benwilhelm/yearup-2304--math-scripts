// gather inputs
var radius = 10;

// perform operation
// area = PI * radius^2
var area = Math.PI * (radius * radius);

// write output
console.log('Radius of Circle:', radius);
console.log('Area of circle', area);
